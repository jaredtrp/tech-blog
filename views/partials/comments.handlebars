<div class="comments ">
    {{#each this}}
    <section class="comment card card-header text-dark bg-primary">
        <div class="meta">
            {{user.username}} on {{format_date createdAt}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>